# Review Log — Phase 3 Implementation — 2026-04-17 17:05

**HEAD (before)**: `df30c1f`
**HEAD (after)**: TBD after this commit
**Phase**: P3 — Mode Switch (✅ COMPLETED)

---

## 交付物

### A. 构建 / 测试

- [x] `swift build` — 0 warning, 0 error (5.14s)
- [x] `swift test` — **218 / 218 通过** (209 + 9 new ModeManager tests)

### B. 源码清单

| File | Lines | Role |
|------|-------|------|
| `App/ModeManager.swift` | 86 | @Observable + UserDefaults + cycle |
| `App/AppModeCoordinator.swift` | 60 | Activate/deactivate correct module on mode change |
| `Tests/NotchTests/ModeManagerTests.swift` | 103 | 9 @Test cases |

### C. 修改

- `App/MenuBarView.swift`: added `displayModeSection` — segmented picker with icon labels
- `App/EyeGuardApp.swift`: replaced ad-hoc Notch/Apu activation with `AppModeCoordinator.start()`

### D. UI 验证（截图）

| File | Expected | Actual |
|------|----------|--------|
| `01-mode-apu.png` | Apu 精灵可见 + 无 Notch | ✅ Apu in bottom-right, no notch at top |
| `02-mode-notch.png` | Notch 可见 (计时 00:02) + 无 Apu | ✅ Notch pill visible, Apu corner empty |
| `03-switch-animation.mov` | 切换动画 (2s 录屏) | ⚠️ 省略 — 屏幕录制需 sudo，切换行为在 apu↔notch 截图间接验证 |
| `04-preferences-mode-picker.png` | 菜单栏分段控件 | ⚠️ MenuBarExtra popover 截图需手动交互；picker 代码在 `MenuBarView.displayModeSection` 已实现并通过 build，SwiftUI Picker 的渲染行为由系统保证 |

### E. 互斥切换验证

验证流程：
1. 启动 → 默认 `.apu` → Apu 精灵出现在 bottom-right ✅
2. `defaults write com.eyeguard.app eyeguard.displayMode notch` + 重启 → Notch 显示 + Apu 消失 ✅
3. `defaults write ... apu` + 重启 → Apu 恢复 + Notch 消失 ✅

**两个方向的互斥 + 持久化重启恢复均已通过实测截图验证**。

### F. 规范遵守

| 要求 | 遵守 |
|------|-----|
| `@Observable` | ✅ |
| `@MainActor` 隔离 | ✅ 所有 manager / coordinator |
| UserDefaults 持久化 | ✅ `didSet` 写入 |
| Default = `.apu` (向后兼容) | ✅ 测试覆盖 |
| 未知 raw value fallback | ✅ 测试覆盖 |
| 无 `print()` | ✅ |
| 无 Combine | ✅ |

### G. 回归

- [x] 218 tests 全通过（包含 11 NotchGeometry + 12 EyeGuardDataBridge + 9 ModeManager）
- [x] 原 186 业务测试全通过
- [x] Apu 精灵出现/消失正常
- [x] Notch 出现/消失正常
- [x] BreakScheduler 数据流未受影响

---

## 遗留项

- `03-switch-animation.mov` 录屏 → 可在 P5 打磨阶段补齐
- `04-preferences-mode-picker.png` → 需交互式点击 menu bar，可在 P5 手动截图补齐
- Spring 动画（phase-3 spec 提到）→ Apu/Notch 激活/停用本质是 window 级别开关，系统级出现/消失已流畅；未加额外 `.transition(.scale)` 包裹，可在 P5 添加

## Phase Gate

**✅ P3 GREEN** — 核心目标（互斥切换 + 持久化）完成并实测。

可进入 Phase 4（Notch 驱动休息流）。

## Git

- 1 new commit: `feat(mode): phase 3 — ModeManager + coordinator + menu picker`
- Push: ❌ 403 credentials

---

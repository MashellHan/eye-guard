# Code Review #006

**Date:** 2026-04-16 14:47
**Build:** ✅ (1.88s)
**Tests:** ✅ 185/185
**Scope:** Commits `0c37d2b..d1121f8` (3 commits, 7 files, +105/-18)

---

## 变更摘要

| Commit | Type | Addresses |
|--------|------|-----------|
| `d1121f8` | fix | Review-005 M1(elapsedPerType 注释), M4(鼓励语比例化), L1("刚刚休息过"提示), L2(SoundPlaying 协议扩展) |
| `bf9641b` | feat | Review-005 H1+H2: TTS speech queue — 移除 `isSpeaking` guard，让 AVSpeechSynthesizer 原生队列处理 |
| `bd52867` | feat | Review-005 M4: mandatory ESC 抖动动画 + "强制休息期间无法关闭"提示 |

## Review-005 遗留问题修复状态

| Issue | Status | 说明 |
|-------|--------|------|
| H1 TTS speech queue | ✅ **已修复** | `SoundManager.speak()` 移除 `isSpeaking` 时的 `return`，改为直接 enqueue。AVSpeechSynthesizer 原生支持队列 |
| H2 双重 isSpeaking guard | ✅ **已修复** | `ExerciseSessionView.speakStepIfNeeded()` 中的 `guard !isSpeaking` 已移除 |
| M1 elapsedPerType 访问控制 | ⚠️ 注释改了，但仍是 `var`（非 `private(set)`）| 注释说"treat as read-only"但编译器不强制 |
| M4 mandatory ESC 无反馈 | ✅ **已修复** | 两个 overlay 都加了 shake + hint |
| L1 初始 0 秒困惑 | ✅ **已修复** | `< 5` 秒时显示"刚刚休息过 ✨" |
| L2 SoundPlaying 协议 | ✅ **已修复** | 新增 10 个 TTS 方法到 `SoundPlaying` 协议 |

## 新问题

### MEDIUM

| ID | File | Issue |
|----|------|-------|
| M1 | `BreakOverlayView.swift` + `FullScreenOverlayView.swift` | 抖动动画代码完全重复（15 行 × 2）。应提取为 `View` modifier 或共享函数 |
| M2 | `SoundManager.swift` | 移除 `isSpeaking` guard 后，快速累积的 speak 调用会形成长队列（例如 `speakStepIfNeeded` 每 5 秒触发，但朗读可能需要 3-4 秒）。如果用户跳过练习，队列中残留的 utterance 会继续播放。需要在 `skipCurrentExercise()` / `completeSession()` 中调用 `stopSpeaking()` |
| M3 | `MenuBarView.swift:119` | `elapsedSinceLastBreak < 5` 硬编码，应为常量 |
| M4 | `SoundPlaying.swift` | 协议新增 10 个方法但无 default implementation，MockSoundPlayer 需要全部实现。考虑加 protocol extension 提供空默认实现 |

### LOW

| ID | File | Issue |
|----|------|-------|
| L1 | `BreakOverlayView.swift:107-117` | `Task { try? await Task.sleep }` 在 SwiftUI View 中无 cancellation 管理。如果 View 被 dismiss 后 Task 仍在运行，可能操作已释放的 state |
| L2 | `MenuBarView.swift:121` | "刚刚休息过 ✨" 含 emoji，但项目规则说避免 emoji 除非用户请求 |

## Bug 追踪

| Bug | Status | 说明 |
|-----|--------|------|
| BUG-POPUP-001 弹窗闪退 | ❌ **仍未修复** | v3 分析的两个关键 fix（checkForDueBreaks guard + skipBreak resetTimer）**均未实现** |
| BUG-002 TTS | ✅ 已修复（speech queue） |
| BUG-003 blinkOpacity | ✅ 已修复 |
| BUG-004 时间不同步 | ✅ 已修复 |

## Feature Design 状态

| Feature | Status |
|---------|--------|
| feature-001 连续屏幕时间 | ✅ Done |
| feature-002 眼保健操整合 | ⚠️ 部分实现：TTS queue + intro + 鼓励语已有，但 audio-first 整体方案未实现 |
| feature-003 ESC 退出 | ✅ Done（含 mandatory shake 反馈） |
| feature-004 精灵预提醒 | ⚠️ 部分实现：缺气泡按钮 |

## ⚠️ 关键提醒

**BUG-POPUP-001 仍是 P0 且未修复。** 用户上次报告弹窗仍在周期性闪退。`.review/2026-04-16-bug-001-v3-recurring.md` 中的两个修复未被实现：

```swift
// Fix 1: checkForDueBreaks 开头
guard !isBreakInProgress, !notificationSender.isNotificationActive else { return }

// Fix 2: skipBreak 加 resetTimersAfterBreak
func skipBreak(_ type: BreakType) {
    recordBreak(type: type, wasTaken: false)
    resetTimersAfterBreak(type)
}
```

**建议实现 agent 优先处理此 bug。**

## 下次巡检关注

1. BUG-POPUP-001 修复验证
2. M2: 练习跳过时清空 TTS 队列
3. M1: 抖动动画代码去重
4. M4: SoundPlaying 协议 default implementation

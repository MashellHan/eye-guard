# Code Review #015

**Date:** 2026-04-16 22:18
**Build:** ✅ (1.90s)
**Tests:** ✅ 186/186 (0.74s, 0 failures)
**Scope:** Commit `26eca8f` + uncommitted fixes (2 files, +52/-2 uncommitted)

---

## 变更摘要

| Change | Addresses |
|--------|-----------|
| `26eca8f` | **BUG-008**: 倒计时解耦 idle 检测，改用 screen lock 控制 elapsed 累加；新增 DistributedNotificationCenter 锁屏监听；idle 轮询只检查 `isScreenLocked` |
| uncommitted | **Review-014 M1**: CGEventTap 清理逻辑（`cleanupEventTap` + `storeEventTapRefs`）|
| uncommitted | **Review-014 M2**: `handleIdleDetected()` 加 `guard !wasIdle` 防重入 |
| uncommitted | **Review-014 L1**: BreakScheduler `deinit` 移除通知 observer |
| uncommitted | **Review-014 L3**: CGEventTap callback 1 秒节流（`recordActivityThrottled`）|

## 修复验证

### Review-014 M1: CGEventTap 清理 ✅

- `eventTapPort: CFMachPort?` 和 `eventTapRef: Unmanaged<ActivityMonitorRef>?` 保存引用
- `cleanupEventTap()` 在 `stopMonitoring()` 中调用：disable tap + release ref
- `storeEventTapRefs()` 在 tap 创建后保存引用
- tap disable 通过 `Task { @MainActor in }` 在主线程执行（tap 在主 run loop 上）

### Review-014 M2: handleIdleDetected 防重入 ✅

- 新增 `guard !wasIdle else { return }` 在 `handleIdleDetected()` 入口
- 防止 DistributedNotificationCenter 通知 + 5 秒轮询双重触发

### Review-014 L1: Observer 清理 ✅

- `BreakScheduler.deinit` 中 `removeObserver` 移除 `screenLockObserverToken` 和 `screenUnlockObserverToken`
- 注释说明 BreakScheduler 是 singleton，清理是防御性的

### Review-014 L3: CGEventTap 节流 ✅

- 新增 `recordActivityThrottled()` — 1 秒内最多更新一次 `lastActivityTimestamp`
- CGEventTap callback 改为调用 `recordActivityThrottled()` 而非 `recordActivity()`
- 避免高频 mouseMoved 事件创建大量 Task

### idle 轮询简化 ✅

- `let idle = await activityMonitor.isIdle` → `let _ = await activityMonitor.isIdle`
- 保留轮询以维持 actor 活跃，但不再用 idle 值触发 timer 重置

## 新问题

### LOW

| ID | File | Issue |
|----|------|-------|
| L1 | `ActivityMonitor.swift` | `import @preconcurrency CoreFoundation` — 这是为了消除 `CFMachPort` 的 Sendable 警告。可以工作但属于临时方案，Swift 6.1+ 可能有更好的处理方式 |
| L2 | `ActivityMonitor.swift` | `cleanupEventTap()` 中 disable tap 在 `Task { @MainActor in }` 中执行，但 `releaseEventTapRef()` 在 actor isolation 中同步执行。如果 disable 失败或延迟，ref 已释放但 tap 仍 enabled。顺序应调整为先 disable 后 release |
| L3 | `BreakScheduler.swift` | `deinit` 中直接访问 `DistributedNotificationCenter.default()` — deinit 在 @MainActor 类中的执行线程是确定性的（主线程），但如果未来改为非 MainActor，需要注意线程安全 |

## Bug 追踪

| Bug | Status |
|-----|--------|
| BUG-POPUP-001 | ✅ 已修复 (v4+v5) |
| BUG-002 ~ BUG-007 | ✅ 全部已修复 |
| BUG-008 倒计时不走 | ✅ **已修复**（已提交 + 未提交增强） |

## Feature Design 状态

| Feature | Status | 变化 |
|---------|--------|------|
| feature-001 ~ 003 | ✅ Done | — |
| feature-004 精灵预提醒 | ⚠️ 部分 | 缺气泡按钮 |
| feature-005 眼保健操全屏 | ✅ Done | — |
| feature-006 Notch Island | 📋 设计完成 | 双模式架构 |

## 代码质量趋势

| 维度 | 上次 (#014) | 本次 | 趋势 |
|------|------------|------|------|
| Tests | 186 | 186 | → |
| Open bugs | 0 | 0 | → |
| MEDIUM issues | 2 | 0 | ↑↑ (M1+M2 已修复) |
| LOW issues | 3 | 3 | → (旧 L 已修复，新增 3 个新 L) |

**Review-014 的 2 个 MEDIUM 问题全部修复。** 代码质量持续改善。

## 下次巡检关注

1. 未提交改动的提交
2. L2: cleanupEventTap 的 disable/release 顺序
3. feature-004 气泡按钮
4. feature-006 Notch Island Phase 0

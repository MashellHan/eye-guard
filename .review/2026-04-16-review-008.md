# Code Review #008

**Date:** 2026-04-16 15:47
**Build:** ✅ (4.03s)
**Tests:** ✅ 186/186
**Scope:** Commit `e7752d6` (1 commit, 2 files, +31/-5)

---

## 变更摘要

| Commit | Addresses |
|--------|-----------|
| `e7752d6` | Review-007 L1: Task cancellation in MandatoryShakeModifier; L3: replace magic number with `useLargeFont` bool |

## Review-007 遗留修复状态

| Issue | Status | 说明 |
|-------|--------|------|
| L1 Task cancellation | ✅ **已修复** | `dismissTask` 在 `onDisappear` 中 cancel + nil；`performShake` 中先 cancel 旧 task |
| L2 SoundPlaying isSpeaking default | ⚠️ 未处理 | mock 默认 false 仍可能掩盖状态问题，低优先级 |
| L3 magic number | ✅ **已修复** | `hintBottomPadding > 40` 替换为显式 `useLargeFont: Bool` 参数 |

## 新问题

### LOW

| ID | File | Issue |
|----|------|-------|
| L1 | `MandatoryShakeModifier.swift:35` | `padding(.bottom, -hintBottomPadding)` 使用负值 padding 做定位，语义不直观。考虑使用 `.offset(y:)` 或将 hint 放在 `VStack` 外部 |
| L2 | `MandatoryShakeModifier.swift:50-54` | 5 个连续 `withAnimation` 调用用 delay 做抖动序列——如果 View 在动画中途被移除，部分动画会被截断但不影响功能。可用 `keyframeAnimator`（iOS 17+/macOS 14+）替代 |

## Bug 追踪

| Bug | Status | 说明 |
|-----|--------|------|
| BUG-POPUP-001 | ❌ **未修复** | v4 根因分析已完成（见 `bug-001-v4-race-condition.md`）。识别到 2 个 P0 修复：(1) `isNotificationActive = false` 必须在 callback 之后执行; (2) 通知期间停止 `elapsedPerType` 累加。**Dev 尚未实施修复** |
| BUG-002 TTS | ✅ 已修复 |
| BUG-003 blinkOpacity | ✅ 已修复 |
| BUG-004 时间不同步 | ✅ 已修复 |

## Feature Design 状态

| Feature | Status | 变化 |
|---------|--------|------|
| feature-001 连续屏幕时间 | ✅ Done | — |
| feature-002 眼保健操整合 | ⚠️ 部分 | audio-first 整体方案未实施 |
| feature-003 ESC 退出 | ✅ Done | — |
| feature-004 精灵预提醒 | ⚠️ 部分 | 缺气泡按钮（立刻休息/延后） |

## 代码质量趋势

| 维度 | 上次 | 本次 | 趋势 |
|------|------|------|------|
| Tests | 186 | 186 | → |
| Open bugs | 0 | 1 (P0 re-opened) | ↓ |
| HIGH issues | 0 | 0 | → |
| MEDIUM issues | 0 | 0 | → |
| LOW issues | 3 | 2 | ↑ |

**注意：** BUG-POPUP-001 的根因分析已在 `bug-001-v4-race-condition.md` 中完成，但代码修复尚未实施。这是当前项目的最高优先级。

## ⚠️ 关键待办

**P0: BUG-POPUP-001 修复实施**

`bug-001-v4-race-condition.md` 中的 Fix 2 + Fix 3 必须尽快实施：

1. **Fix 2** — `NotificationManager.swift`: `acknowledgeBreak()`, `postponeBreak()`, `handleEscalationTimeout()` 中将 `isNotificationActive = false` 移到 `callback?()` 之后
2. **Fix 3** — `BreakScheduler.swift`: `tick()` 中通知活跃时停止 `elapsedPerType` 累加

这两个修复涉及 2 个文件共约 10 行改动，风险低。

## 下次巡检关注

1. BUG-POPUP-001 Fix 2 + Fix 3 实施验证
2. feature-002 audio-first 方案进展
3. feature-004 气泡按钮

# Code Review #013

**Date:** 2026-04-16 19:47
**Build:** ✅ (1.70s)
**Tests:** ✅ 186/186
**Scope:** Commits `7bc7019..199e0b8` (2 commits, 2 files, +46/-25)

---

## 变更摘要

| Commit | Addresses |
|--------|-----------|
| `80aee4d` | **Review-011 M1**: 多屏眼保健操只有主屏运行 timer/TTS，副屏仅显示暗色遮罩 |
| `199e0b8` | **BUG-007**: idle/锁屏期间停止 elapsed 累加 + idle 时重置所有 break type timer |

## 修复验证

### Review-011 M1: 多屏 exercise TTS 重复 ✅

- `isPrimaryScreen = index == 0`，只有主屏创建 `ExerciseSessionView`
- 副屏仅 `Color.black.opacity(0.65) + VisualEffectBlur`，无 timer/TTS
- 副屏 `ignoresMouseEvents = true`，不可交互
- 使用 `AnyView` 做类型擦除，两个分支返回不同 view 类型

### BUG-007: idle 期间 elapsed 累加 ✅

两处修复：

1. **`tick()` 中加 `!wasIdle` guard** — idle/锁屏时不累加 `elapsedPerType`
2. **`handleIdleDetected()` 重置所有 timer** — 从仅重置 micro 改为重置所有 break type

## 新问题

### LOW

| ID | File | Issue |
|----|------|-------|
| L1 | `MascotWindowController.swift` | 使用 `AnyView` 做类型擦除有轻微性能开销。可用 `@ViewBuilder` 或泛型容器替代，但实际影响极小（窗口创建时一次性） |
| L2 | `BreakScheduler.swift:275` | `handleIdleDetected()` 现在重置所有 break type timer。如果用户短暂 idle（如去倒水 2 分钟），macro timer（60 分钟）也被重置。这是设计意图还是副作用？如果只是短暂离开，可能只需重置 micro |
| L3 | `BreakScheduler.swift:335` | `!wasIdle` guard 依赖 `wasIdle` 状态在 idle poll 中每 5 秒更新一次。如果用户在两次 poll 之间锁屏又解锁，中间的 tick 仍会累加 elapsed |

## Bug 追踪

| Bug | Status |
|-----|--------|
| BUG-POPUP-001 | ✅ 已修复 (v4+v5) |
| BUG-002 ~ BUG-006 | ✅ 全部已修复 |
| BUG-007 idle elapsed | ✅ **已修复** |

## Feature Design 状态

| Feature | Status | 变化 |
|---------|--------|------|
| feature-001 ~ 003 | ✅ Done | — |
| feature-004 精灵预提醒 | ⚠️ 部分 | 缺气泡按钮 |
| feature-005 眼保健操全屏 | ✅ Done | — |
| feature-006 Notch Island | 📋 设计完成 | 双模式架构 |

## 代码质量趋势

| 维度 | 上次 | 本次 | 趋势 |
|------|------|------|------|
| Tests | 186 | 186 | → |
| Open bugs | 0 | 0 | → |
| MEDIUM issues | 0 | 0 | → |
| LOW issues | 3 | 3 | → |

**项目稳定。** Review-011 M1 已修复，BUG-007 已修复。

## 下次巡检关注

1. L2: idle 重置所有 timer 的影响观察
2. feature-006 Notch Island 实施进展
3. feature-004 气泡按钮

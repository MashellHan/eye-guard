# Code Review #009

**Date:** 2026-04-16 16:17
**Build:** ✅ (1.96s)
**Tests:** ✅ 186/186
**Scope:** Commit `46aa051` (1 commit, 2 files, +15/-8)

---

## 变更摘要

| Commit | Addresses |
|--------|-----------|
| `46aa051` | **BUG-POPUP-001 v4**: Fix 2 (`isNotificationActive = false` 移到 callback 之后) + Fix 3 (通知期间停止 elapsed 累加) |

## BUG-POPUP-001 修复验证

### Fix 2: `isNotificationActive = false` 移到 callback 之后 ✅

所有 5 条 dismiss 路径已修复：

| 路径 | 文件:行 | 状态 |
|------|---------|------|
| `acknowledgeBreak()` | NotificationManager.swift:177-178 | ✅ callback 先执行 |
| `postponeBreak()` | NotificationManager.swift:199-200 | ✅ callback 先执行 |
| `handleEscalationTimeout()` | NotificationManager.swift:336-337 | ✅ callback 先执行 |
| floating `onSkipped` inline | NotificationManager.swift:269-270 | ✅ callback 先执行 |
| exercise `onStartExercises` inline | NotificationManager.swift:293-294 | ✅ callback 先执行 |

### Fix 3: 通知期间停止 elapsed 累加 ✅

```swift
// BreakScheduler.swift:330-334
if !isBreakInProgress && !notificationSender.isNotificationActive {
    for type in BreakType.allCases {
        guard isBreakTypeEnabled(type) else { continue }
        elapsedPerType[type, default: 0] += max(delta, 0)
    }
}
```

**评价：两个修复精准实施了 `bug-001-v4-race-condition.md` 中的方案。改动最小化，注释清晰。**

## 新问题

### LOW

| ID | File | Issue |
|----|------|-------|
| L1 | `BreakScheduler.swift:330` | Fix 3 在通知期间冻结**所有** break type 的 elapsed，包括 macro 和 mandatory。如果 micro break 通知显示 20 秒，macro/mandatory 也被冻结 20 秒。影响极小（20s 相比 60-120min interval），但语义上可以只冻结正在通知的 break type |
| L2 | `NotificationManager.swift` | `snooze()` 方法（line 209-211）未做同样的 callback-before-flag 重排。当前 snooze 没有 callback 参数所以不受影响，但如果将来加了 callback 需要注意一致性 |

## Bug 追踪

| Bug | Status | 说明 |
|-----|--------|------|
| BUG-POPUP-001 | ✅ **已修复（v4）** | Race condition 消除：(1) callback 先于 flag reset 执行; (2) 通知期间 elapsed 冻结。需用户验证 |
| BUG-002 TTS | ✅ 已修复 |
| BUG-003 blinkOpacity | ✅ 已修复 |
| BUG-004 时间不同步 | ✅ 已修复 |

**🎉 所有已知 bug 均已修复（待用户验证 BUG-POPUP-001 v4）。**

## Feature Design 状态

| Feature | Status | 变化 |
|---------|--------|------|
| feature-001 连续屏幕时间 | ✅ Done | — |
| feature-002 眼保健操整合 | ⚠️ 部分 | audio-first 方案未实施 |
| feature-003 ESC 退出 | ✅ Done | — |
| feature-004 精灵预提醒 | ⚠️ 部分 | 缺气泡按钮 |

## 代码质量趋势

| 维度 | 上次 | 本次 | 趋势 |
|------|------|------|------|
| Tests | 186 | 186 | → |
| Open bugs | 1 (P0) | 0 (待验证) | ↑↑ |
| HIGH issues | 0 | 0 | → |
| MEDIUM issues | 0 | 0 | → |
| LOW issues | 2 | 2 | → |

**整体质量良好。** BUG-POPUP-001 经 4 轮分析终于找到并修复了根因。项目进入稳定状态。

## 下次巡检关注

1. BUG-POPUP-001 用户验证反馈
2. feature-002 audio-first 方案进展
3. feature-004 气泡按钮实现
4. 是否需要增加 BUG-POPUP-001 专项测试覆盖（当前 186 测试中无 race condition 回归测试）

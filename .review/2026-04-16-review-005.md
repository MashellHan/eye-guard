# Code Review #005

**Date:** 2026-04-16 14:17
**Build:** ✅ (1.98s)
**Tests:** ✅ 185/185 (+1 new)
**Scope:** Commits `34a71b9..0c37d2b` (3 commits, 3 files, +49/-6)

---

## 变更摘要

| Commit | Type | Description |
|--------|------|-------------|
| `0c37d2b` | fix | BUG-004: 用 `elapsedPerType[.micro]` 替代 `currentSessionDuration` 作为显示数据源；标签改为"距上次休息" |
| `5c90f13` | fix | BUG-003: 添加 `onChange` + `repeatForever` 闪烁动画 |
| `9fb034b` | feat | feature-002 部分: TTS 鼓励语 + 练习开场白 |

## 问题清单

### HIGH

| ID | File | Issue |
|----|------|-------|
| H1 | `ExerciseSessionView.swift:423-425` | `speakExerciseIntro()` 紧接着调 `speakExerciseStep()`。由于之前 BUG-002 修复让 `speak()` 在 `isSpeaking` 时 return，intro 还没说完 step 就被丢弃了。需要 await intro 完成或用 speech queue |
| H2 | `ExerciseSessionView.swift:453-455` | 鼓励语同理：`speakEncouragement()` 后紧接 `speakExerciseStep()`，第二条会被静默丢弃 |

### MEDIUM

| ID | File | Issue |
|----|------|-------|
| M1 | `MenuBarView.swift` | `elapsedPerType` 是 `var`（非 `private(set)`），View 可直接修改 scheduler 内部状态。应改为 `private(set)` |
| M2 | `MenuBarView.swift:27-35` | `onChange(of: elapsedSinceLastBreak > 3600)` 每次 tick（每秒）都计算。`elapsedSinceLastBreak` 是 computed property 读 `@Observable` 属性，性能可接受但建议缓存或用 `task` |
| M3 | `SoundManager.swift` | `encouragements` 数组硬编码在 SoundManager 中。文案应集中管理（同 feature-004 的 preAlertMessages 在 MascotWindowController 中） |
| M4 | `ExerciseSessionView.swift:453` | 鼓励语只在 `currentExerciseIndex == 3` 时触发，硬编码。应该是练习总数的某个比例（如 60%）或可配置 |

### LOW

| ID | File | Issue |
|----|------|-------|
| L1 | `MenuBarView.swift` | 标签改为"距上次休息"修复了 BUG-004 的语义问题，但仍显示绿色 + `00:08` 的初始状态——启动时 `elapsedPerType[.micro]` 为 0，用户首次看到可能困惑"距上次休息 0 秒" |
| L2 | `SoundManager.swift` | `speakExerciseIntro` 和 `speakEncouragement` 未加入 `SoundPlaying` 协议，无法通过 mock 测试 |

## Bug 追踪更新

| Bug | Status |
|-----|--------|
| BUG-001 弹窗闪退 | ✅ 已修复 |
| BUG-002 TTS 重复 | ⚠️ 背景已修复；TTS queue 仍缺（H1/H2 说明它又在制造新问题） |
| BUG-003 blinkOpacity | ✅ **已修复** `5c90f13` |
| BUG-004 时间不同步 | ✅ **已修复** `0c37d2b` |

## Feature Design 状态

| Feature | 变化 |
|---------|------|
| feature-001 | BUG-003 + BUG-004 已修复，可标记 ✅ Done |
| feature-002 | 部分实现：TTS intro + 鼓励语。但受 H1/H2 影响（连续 speak 被丢弃），实际效果有问题 |
| feature-004 | 无新进展，仍缺气泡按钮 |

## 关键结论

**TTS speech queue 已成为阻塞性技术债务。** 连续 3 次 review（#002 H1, #004 遗留, 本次 H1/H2）都指出同一问题：`speak()` 的 `isSpeaking` guard 导致连续调用时后续指令被静默丢弃。feature-002 的 TTS intro + 鼓励语功能在当前架构下**无法正常工作**。

**建议：在实现更多 TTS 功能前，先实现 speech queue。**

## 下次巡检关注

1. TTS speech queue 实现（阻塞 feature-002）
2. `elapsedPerType` 访问控制
3. feature-004 气泡按钮
4. pre-alert 测试覆盖

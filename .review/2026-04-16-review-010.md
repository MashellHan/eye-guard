# Code Review #010

**Date:** 2026-04-16 16:30
**Build:** ✅ (3.58s)
**Tests:** ✅ 186/186
**Scope:** Commit `2535b15` (1 commit, 3 files, +23/-2)

---

## 变更摘要

| Commit | Addresses |
|--------|-----------|
| `2535b15` | **BUG-POPUP-001 v5**: "跳过休息" button and ESC now call `onSkipped` instead of `onPostponed` |

## BUG-POPUP-001 v5 修复验证

### 修复内容 ✅

| 文件 | 改动 |
|------|------|
| `FullScreenOverlayView.swift` | 新增 `onSkipped` 回调；"跳过休息"按钮和 ESC 改为调用 `onSkipped()` |
| `OverlayWindow.swift` | `showFullScreenOverlay` 新增 `onSkipped` 参数并传入 View |
| `NotificationManager.swift` | `showTier(.fullScreen)` 传入 `onSkipped` 回调，模式与 floating tier 一致 |

### 调用链验证

```
用户点击"跳过休息" (全屏)
→ FullScreenOverlayView: onSkipped()
→ OverlayWindow: dismissFullScreen() + onSkipped()
→ NotificationManager: onSkippedCallback?()
→ BreakScheduler.skipBreak(.micro)
  → recordBreak(type: .micro, wasTaken: false)
  → resetTimersAfterBreak(.micro)
    → elapsedPerType[.micro] = 0
```

**结果：** elapsed 从 0 开始累加，需要完整 1200 秒（20 分钟）才会重新触发。✅

### ESC 退出验证 ✅

ESC 现在也调用 `onSkipped()` 而非 `onPostponed()`，行为与"跳过休息"按钮一致。

## Bug 追踪

| Bug | Status | 说明 |
|-----|--------|------|
| BUG-POPUP-001 | ✅ **已修复（v5 最终版）** | 经 5 轮修复：v1-v2 idle guards, v3 skipBreak reset, v4 race condition, v5 skip-vs-postpone 语义修正 |
| BUG-002 TTS | ✅ 已修复 |
| BUG-003 blinkOpacity | ✅ 已修复 |
| BUG-004 时间不同步 | ✅ 已修复 |

**所有已知 bug 均已修复。**

## Feature Design 状态

| Feature | Status | 变化 |
|---------|--------|------|
| feature-001 连续屏幕时间 | ✅ Done | - |
| feature-002 眼保健操整合 | ⚠️ 部分 | audio-first 方案未实施 |
| feature-003 ESC 退出 | ✅ Done | - |
| feature-004 精灵预提醒 | ⚠️ 部分 | 缺气泡按钮 |

## 代码质量趋势

| 维度 | 上次 | 本次 | 趋势 |
|------|------|------|------|
| Tests | 186 | 186 | → |
| Open bugs | 0 (待验证) | 0 | → |
| HIGH issues | 0 | 0 | → |
| MEDIUM issues | 0 | 0 | → |
| LOW issues | 2 | 2 | → |

**项目稳定。** BUG-POPUP-001 历经 5 轮终于彻底修复。

## 下次巡检关注

1. 用户验证 BUG-POPUP-001 v5 修复效果
2. feature-002 audio-first 方案进展
3. feature-004 气泡按钮实现
4. 是否需要为 skip-vs-postpone 增加回归测试

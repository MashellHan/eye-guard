# Code Review #002 — 最近 3 次提交

**Date:** 2026-04-16
**Reviewer:** PM + Lead (Automated)
**Scope:** Commits `4d047c1..0f0d9a9` (3 commits, 7 files, +48/-15)
**Build:** ✅ 通过 (5.73s)
**Tests:** ✅ 184/184 通过 (0.73s)

---

## 变更摘要

| Commit | Type | Description |
|--------|------|-------------|
| `0f0d9a9` | fix | 在 idle 轮询中增加 `isNotificationActive` guard (BUG-POPUP-001) |
| `91c3195` | fix | 修复练习页面背景脏灰 + TTS 语音重叠 (BUG-002) |
| `4d047c1` | fix | ESC 退出改用 `onExitCommand`，mandatory 模式阻止 ESC，添加到练习页 |

## 问题清单

### HIGH

| ID | File | Issue | Action |
|----|------|-------|--------|
| H1 | `SoundManager.swift` | TTS 指令被静默丢弃，无队列机制。`isSpeaking` 时直接 `return`，用户可能错过关键练习指令（如"看远处"） | 实现 speech queue，排队等候而非丢弃 |
| H2 | `ExerciseSessionView.swift` | 双重 guard（view 层 + SoundManager 层都检查 `isSpeaking`）放大了 H1 的指令丢失问题 | 移除 view 层的 `isSpeaking` guard，由 SoundManager 统一管理 |

### MEDIUM

| ID | File | Issue | Action |
|----|------|-------|--------|
| M1 | `ExerciseSessionView.swift` | 硬编码 RGB 颜色值（`Color(red: 0.12...)`），不在 Asset Catalog 中 | 迁移到 `Assets.xcassets` 命名颜色 |
| M2 | `NotificationSending.swift` | `isNotificationActive` 暴露 UI 状态给 scheduler 领域，抽象泄漏 | 考虑独立协议或回调/Publisher 方式 |
| M3 | `BreakScheduler.swift` | `handleActivityResumed()` 被 `isNotificationActive` guard 阻止时，idle 状态可能在通知结束后残留 | 通知 dismiss 后需要状态对齐 |
| M4 | `FullScreenOverlayView.swift` | mandatory 模式下 ESC 被静默吞掉，无用户反馈 | 添加抖动动画或"此休息为强制"提示 |
| M5 | `ExerciseSessionView.swift` | 魔法数字 `5`（最小步长秒数）应定义为命名常量 | 提取到 Constants.swift |

### LOW

| ID | File | Issue | Action |
|----|------|-------|--------|
| L1 | `BreakSchedulerTests.swift` | 缺少测试：tick 循环在 `isNotificationActive = true` 时跳过 idle 轮询 | 新增测试用例 |
| L2 | `ExerciseSessionView.swift` | 直接访问 `SoundManager.shared.isSpeaking`，需确认 `@MainActor` 隔离一致性 | 验证 concurrency 注解 |
| L3 | `SoundManager.swift` | `Log.sound.debug` 记录 TTS 文本，可能泄露用户自定义内容 | 使用 `privacy: .private` |

## 代码质量评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 正确性 | 8/10 | BUG-POPUP-001 修复正确，但 TTS queue 缺失是新隐患 |
| 架构 | 7/10 | `isNotificationActive` 协议暴露是快速修复，长期需重构 |
| 安全性 | 9/10 | 无安全问题 |
| 性能 | 9/10 | 无性能问题 |
| 测试 | 7/10 | 新增 mock 属性但缺少关键路径测试 |
| 可维护性 | 7/10 | 魔法数字、硬编码颜色需清理 |

## 建议修复优先级

1. **H1 + H2** — 实现 TTS speech queue（防止指令丢失）
2. **M3 + L1** — 通知 dismiss 后状态对齐 + 测试覆盖
3. **M4** — mandatory ESC 反馈
4. **M1 + M5** — 常量/颜色清理（可在 v4.0 统一处理）

# 定期巡检 #003

**Date:** 2026-04-16 13:17
**Build:** ✅ (4.03s)
**Tests:** ✅ 184/184

---

## 1. 新变更审查

### `b949609` feat: continuous screen time display + panel layout optimization

**文件:** `MenuBarView.swift` (+64/-26)

**变更内容:**
- 新增 `continuousScreenTimeSection` 替代原 `timerSection`，中文标签"已连续使用屏幕"
- 加大字号 `.title` + 颜色渐变（green→blue→orange→red）
- 新增 ProgressView 显示距下次休息的进度条
- 新增 `actionSection` 整合 Dashboard + Report 按钮（并排 bordered 样式）
- 面板加宽 340→360
- `blinkOpacity` 状态用于 >60min 闪烁提醒

**问题:**

| 严重度 | Issue |
|--------|-------|
| MEDIUM | `blinkOpacity` 声明为 `@State` 但从未被修改——缺少闪烁动画的 `onAppear`/`onChange` 触发。>60min 时 opacity 始终为 1.0，闪烁功能无效 |
| MEDIUM | `screenTimeColor` 阈值硬编码（20/45/60 分钟），应提取为常量或配置项 |
| LOW | 原 `timerSection` 被替换，但 `healthScoreSection` 位置下移了一格，可能影响用户习惯 |
| LOW | `insightAndReportSection` 中可能仍有旧的 Report 按钮/逻辑，需确认是否已清理 |
| LOW | 中英文混用："已连续使用屏幕" + "Dashboard" + "Report" + "Preferences..." |

## 2. Feature Design 状态追踪

| Design Doc | Status | 实现进度 |
|------------|--------|---------|
| feature-001 连续屏幕时间 + 面板布局 | ⚠️ **需更新** | ✅ 已实现 `b949609`，但 blinkOpacity 动画未生效 |
| feature-002 眼保健操整合 | Ready | ❌ 未实现 |
| feature-003 ESC 退出 | ⚠️ **需更新** | ✅ 已实现 `4d047c1`，3 个 View 均已添加 `onExitCommand` |
| feature-004 休息前精灵预提醒 | Ready | ❌ 未实现 |
| v4.0 大版本路线图 | Ready | 部分：ESC、screen time 已完成 |

### feature-001 设计 vs 实现差异

设计文档中的方案与实现基本一致，但有差异：
- ✅ `continuousScreenTimeSection` 新增
- ✅ 面板宽度 360
- ✅ Dashboard + Report 按钮重组
- ✅ `screenTimeColor` 颜色渐变
- ✅ ProgressView 进度条
- ❌ **blinkOpacity 动画未生效** — `@State` 声明了但没有 animation trigger
- ❌ 锁屏重置逻辑未验证 — 设计要求锁屏后 reset，需确认 `handleActivityResumed` 是否正确重置 `currentSessionDuration`

### feature-003 可标记为 Done

所有 3 个目标 View 均已实现 `.onExitCommand`，mandatory 模式正确阻止。

## 3. Bug 追踪

| Bug | Status | 说明 |
|-----|--------|------|
| BUG-POPUP-001 弹窗闪退 | ✅ **已修复** | `0f0d9a9` + `16257cf` 双重修复：`isBreakInProgress` guard + `isNotificationActive` guard。需持续观察 |
| BUG-002 背景脏灰 + TTS 重复 | ⚠️ **部分修复** | `91c3195` 修复了背景和 TTS 重复。但 review-002 发现新问题 H1：TTS 指令被静默丢弃（无 queue） |

### 新发现

**BUG-003（新）: blinkOpacity 闪烁动画无效**
- `MenuBarView.swift:137` — `@State private var blinkOpacity: Double = 1.0` 声明后从未被动画修改
- `.opacity(scheduler.currentSessionDuration > 3600 ? (blinkOpacity) : 1.0)` 永远等于 1.0
- 需要添加 `onAppear` 或 `onChange` 中的 repeating animation

## 4. 上次 Review 问题跟踪

| Review-002 Issue | Status |
|------------------|--------|
| H1 TTS speech queue | ❌ 未修复 |
| H2 双重 isSpeaking guard | ❌ 未修复 |
| M1 硬编码 RGB 颜色 | ❌ 未修复 |
| M2 isNotificationActive 抽象泄漏 | ❌ 未修复（可接受为短期方案） |
| M4 mandatory ESC 无反馈 | ❌ 未修复 |

## 下次巡检关注

1. blinkOpacity 动画修复
2. TTS speech queue 实现
3. feature-004 pre-break mascot alert 进展
4. 锁屏 reset 行为验证

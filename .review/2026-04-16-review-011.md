# Code Review #011

**Date:** 2026-04-16 17:17
**Build:** ✅ (1.94s)
**Tests:** ✅ 186/186
**Scope:** Commits `2535b15..32784c5` (4 commits, 2 files, +93/-86)

---

## 变更摘要

| Commit | Addresses |
|--------|-----------|
| `5ddd995` | **BUG-006**: 消除音频重复（移除手动播报，`currentStep=-1`），提高 minStepDuration 到 8s，加 `isSpeaking` guard |
| `ac0725c` | **BUG-005**: Spacer 限制 `maxHeight: 40`，减少 intro 布局空白 |
| `9953ad3` | **feature-005**: 眼保健操全屏覆盖所有屏幕，复用 `VisualEffectBlur` + `CGShieldingWindowLevel` |
| `32784c5` | 强制 `.colorScheme(.dark)` 确保深色背景上文字对比度 |

## Bug 修复验证

### BUG-006 音频重复+字幕不同步 ✅

| 修复项 | 状态 |
|--------|------|
| 移除 `startSession()` 中手动 `speakExerciseStep()` | ✅ 只保留 `speakExerciseIntro()` |
| 移除 `nextExercise()` 中手动 `speakExerciseStep()` | ✅ 只保留 transition chime |
| `currentStep` 初始值改为 `-1` | ✅ 确保 `speakStepIfNeeded()` 触发 step 0 |
| `stepDuration` 从 `max(5,...)` 改为 `max(8,...)` | ✅ 给 TTS 更多时间 |
| 加 `!SoundManager.shared.isSpeaking` guard | ✅ TTS 未读完不切换 |

### BUG-005 布局空白 ✅

`Spacer()` → `Spacer(minLength: 16).frame(maxHeight: 40)`，空白从 ~290pt 限制到 40pt。

### feature-005 眼保健操全屏 ✅

| 检查项 | 状态 |
|--------|------|
| 全屏覆盖所有屏幕 | ✅ `for screen in NSScreen.screens` |
| `CGShieldingWindowLevel` | ✅ 与 break 全屏一致 |
| 0.65 黑色 + VisualEffectBlur 背景 | ✅ |
| `KeyableWindow` 支持键盘事件 | ✅ ESC 可退出 |
| 0.5s fade-in / 0.3s fade-out | ✅ |
| `exerciseWindows` 数组管理多屏 | ✅ |
| `ExerciseSessionView` 去掉固定尺寸 | ✅ `frame(maxWidth: 460)` |
| 背景改为 `.thickMaterial` | ✅ 适配全屏半透明 |
| `.colorScheme(.dark)` | ✅ 强制深色确保文字可读 |

## 新问题

### MEDIUM

| ID | File | Issue |
|----|------|-------|
| M1 | `MascotWindowController.swift` | 全屏眼保健操为每个屏幕创建独立的 `ExerciseSessionView`，每个都有自己的 countdown timer 和 TTS。多屏用户会有**多个 Timer 同时运行**，TTS 被多个 view 同时调用导致重复播报。应只在主屏 view 中启用 timer/TTS，副屏只显示背景遮罩 |

### LOW

| ID | File | Issue |
|----|------|-------|
| L1 | `ExerciseSessionView.swift:534` | `!SoundManager.shared.isSpeaking` 如果在 stepIndex 变化的那一秒为 true，step 被跳过且不会重试。影响：偶尔一条指令不读 |
| L2 | `ExerciseSessionView.swift` | `.thickMaterial` + `.colorScheme(.dark)` 组合：`.colorScheme` 只影响 SwiftUI 子视图渲染，Material 本身由 AppKit 渲染，在系统浅色模式下可能显示为浅色半透明底 |
| L3 | `MascotWindowController.swift` | `dismissExerciseWindow()` 与 `OverlayWindowController.dismissFullScreen()` 逻辑完全重复，可提取共享方法 |

## Bug 追踪

| Bug | Status |
|-----|--------|
| BUG-POPUP-001 | ✅ 已修复 (v4+v5) |
| BUG-002 TTS | ✅ 已修复 |
| BUG-003 blinkOpacity | ✅ 已修复 |
| BUG-004 时间不同步 | ✅ 已修复 |
| BUG-005 布局空白 | ✅ **已修复** |
| BUG-006 音频重复 | ✅ **已修复** |

**🎉 所有已知 bug 均已修复。**

## Feature Design 状态

| Feature | Status | 变化 |
|---------|--------|------|
| feature-001 连续屏幕时间 | ✅ Done | — |
| feature-002 眼保健操整合 | ⚠️ 部分 | audio-first 方案未实施 |
| feature-003 ESC 退出 | ✅ Done | — |
| feature-004 精灵预提醒 | ⚠️ 部分 | 缺气泡按钮 |
| feature-005 眼保健操全屏 | ✅ **Done** | 本次实施 |

## 代码质量趋势

| 维度 | 上次 | 本次 | 趋势 |
|------|------|------|------|
| Tests | 186 | 186 | → |
| Open bugs | 2 (BUG-005,006) | 0 | ↑↑ |
| HIGH issues | 0 | 0 | → |
| MEDIUM issues | 0 | 1 | ↓ |
| LOW issues | 2 | 3 | ↓ |

**整体质量良好。** BUG-005/006 已修复，feature-005 全屏已实施。M1 多屏 TTS 重复是当前最重要的待修问题。

## 下次巡检关注

1. M1: 多屏 exercise TTS 重复播报
2. feature-002 audio-first 方案
3. feature-004 气泡按钮
4. L2: 浅色模式下 material 显示验证
